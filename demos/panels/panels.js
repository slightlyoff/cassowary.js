document.solver = new c.SimplexSolver();

/*
Panel = c.inherit({
  extends: HTMLDivElement,
  initialize: function() {
    this.variables = {
      width: new c.Variable(this.getId() + "_width");
      height
      left
      right
      top
      bottom
      contentWidth
      contentHeight
      contentLeft
      contentRight
      contentTop
      contentBottom
    };

    // TODO(slightlyoff):
    //      Add zero bounds constraints and sanity checking constraints
  },

  onSolved: function() {
    this.style.... = outcome
  }
});
*/
